name: wot_statistic
description: World of tanks account statistics #Flutter

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

  # State
  flutter_bloc: ^8.0.1
  bloc: ^8.0.1
  equatable: ^2.0.3

  # Network

  # Local

flutter:

  uses-material-design: true

  assets:
    - assets/realm/
    - assets/background/